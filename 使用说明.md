# TCF Oral C1 App 使用说明

这个包里已经包含网页和 138 个本地 mp3 音频，不包含 API key。

电脑使用：
1. 解压 zip。
2. 双击 `index.html`。
3. 点击卡片里的“朗读”，会优先播放本地音频。

手机使用：
最推荐把整个 `tcf-oral-c1-app` 文件夹上传到 Netlify、Vercel、Cloudflare Pages 或 GitHub Pages，然后把网址发给朋友。手机打开网址后，可以在浏览器菜单里选择“添加到主屏幕”，像 app 一样使用。

注意：
- 直接把 zip 发到手机上通常不够丝滑，因为手机浏览器不一定允许网页读取解压后的本地音频。
- 如果要让朋友手机稳定使用，最好发一个网页链接，而不是只发 zip。
- 这个包不需要 ElevenLabs API，也不会消耗你的额度。
